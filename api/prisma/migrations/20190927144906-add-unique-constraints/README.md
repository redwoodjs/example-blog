# Migration `20190927144906-add-unique-constraints`

This migration has been generated by Rob Cameron at 9/27/2019, 2:49:06 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new__PostToTag"("A" INTEGER    REFERENCES "Post"(id) ON DELETE CASCADE,"B" INTEGER    REFERENCES "Tag"(id) ON DELETE CASCADE)
;

INSERT INTO "new__PostToTag" ("A","B") SELECT "A","B" from "_PostToTag"

DROP TABLE "lift"."_PostToTag";

ALTER TABLE "lift"."new__PostToTag" RENAME TO "_PostToTag";

CREATE UNIQUE INDEX "lift"."_PostToTag_AB_unique" ON "_PostToTag"("A","B")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;

CREATE UNIQUE INDEX "lift"."Post.slug" ON "Post"("slug")

CREATE UNIQUE INDEX "lift"."Tag.name" ON "Tag"("name")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190927134833-remove-default-posted-at..20190927144906-add-unique-constraints
--- datamodel.dml
+++ datamodel.dml
@@ -13,9 +13,9 @@
 model Post {
   id Int @id
   title String
-  slug String
+  slug String @unique
   author String
   body String
   image String?
   tags Tag[]
@@ -23,9 +23,9 @@
 }
 model Tag {
   id Int @id
-  name String
+  name String @unique
   posts Post[]
 }
 model User {
```

## Photon Usage

You can use a specific Photon built for this migration (20190927144906-add-unique-constraints)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190927144906-add-unique-constraints'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
