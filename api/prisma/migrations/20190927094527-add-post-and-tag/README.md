# Migration `20190927094527-add-post-and-tag`

This migration has been generated by Rob Cameron at 9/27/2019, 9:45:27 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Post"("body" TEXT NOT NULL DEFAULT ''  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,"image" TEXT    ,"postedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,"slug" TEXT NOT NULL DEFAULT ''  ,"title" TEXT NOT NULL DEFAULT ''  )
;

CREATE TABLE "lift"."Tag"("id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,"name" TEXT NOT NULL DEFAULT ''  )
;

CREATE TABLE "lift"."_PostToTag"("A" INTEGER    REFERENCES "Post"(id) ON DELETE CASCADE,"B" INTEGER    REFERENCES "Tag"(id) ON DELETE CASCADE)
;

CREATE UNIQUE INDEX "lift"."_PostToTag_AB_unique" ON "_PostToTag"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190829094449-add-user..20190927094527-add-post-and-tag
--- datamodel.dml
+++ datamodel.dml
@@ -10,8 +10,24 @@
 // Define your own models here and run yarn db:save to create
 // migrations for them.
+model Post {
+  id Int @id
+  title String
+  slug String
+  body String
+  image String?
+  tags Tag[]
+  postedAt DateTime @default(now())
+}
+
+model Tag {
+  id Int @id
+  name String
+  posts Post[]
+}
+
 model User {
   id Int @id
   name String?
   email String @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20190927094527-add-post-and-tag)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190927094527-add-post-and-tag'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
