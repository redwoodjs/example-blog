# Migration `20190829094449-add-user`

This migration has been generated by Peter Pistorius at 8/29/2019, 9:44:49 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User"("id" INTEGER NOT NULL DEFAULT 0 ,"name" TEXT   ,"email" TEXT NOT NULL DEFAULT '' ,"isAdmin" BOOLEAN NOT NULL DEFAULT false ,PRIMARY KEY ("id"))
;

CREATE UNIQUE INDEX "lift"."User.email._UNIQUE" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190829094449-add-user
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,19 @@
+datasource sqlite {
+  url = "file:dev.db"
+  provider = "sqlite"
+}
+
+generator photonjs {
+  provider = "photonjs"
+  output = "../generated/photon"
+}
+
+// Define your own models here and run yarn db:save to create
+// migrations for them.
+
+model User {
+  id Int @id
+  name String?
+  email String @unique
+  isAdmin Boolean @default(false)
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190829094449-add-user)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190829094449-add-user'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
