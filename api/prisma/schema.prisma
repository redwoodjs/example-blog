datasource hammerDatasource {
  provider = "sqlite"
  url = env("PRISMA_DS_URL") // "file:./dev.db"
}

generator photonjs {
  provider = "photonjs"
  output = "../../packages/photon"
  binaryTargets = ["darwin", "debian-openssl-1.0.x"]
}

// Define your own models here and run yarn db:save to create
// migrations for them.

model Post {
  id Int @id
  title String
  slug String @unique
  author String
  body String
  image String?
  tags Tag[]
  postedAt DateTime?
}

model Tag {
  id Int @id
  name String @unique
  posts Post[]
}

model User {
  id Int @id
  name String?
  email String @unique
  isAdmin Boolean @default(false)
}
