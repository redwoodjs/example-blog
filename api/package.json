{
  "private": true,
  "name": "api",
  "version": "0.0.0",
  "dependencies": {
    "@generated/photon": "0.0.0-generated",
    "@hammerframework/hammer-api": "^0.0.1-alpha.12.5",
    "graphql-iso-date": "^3.6.1",
    "nexus": "v0.12.0-beta.14",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@hammerframework/hammer-dev-server": "^0.0.1-alpha.12",
    "prisma2": "2.0.0-preview015"
  },
  "scripts": {
    "dev": "yarn hammer-dev-server",
    "db:up": "yarn prisma2 lift up && yarn prisma2 generate",
    "db:save": "yarn prisma2 lift save",
    "seed": "node prisma/seeds.js",
    "build": "NODE_ENV=production babel src --out-dir dist"
  }
}
