# Why is this here?

This `@generated/photon` package exists because we need to reference
Photon as an actual depenedency in order to have it bundled properly during
packaging for deployment to production.
