# These environment variables will be used by default if you do not create any
# yourself in .env. This file should be safe to check into your version control
# system. Any custom values should go in .env and .env should *not* be checked
# into version control.

# schema.prisma defaults
DATABASE_URL=file:./dev.db

# location of the test database for api service scenarios (defaults to ./.redwood/test.db if not set)
# TEST_DATABASE_URL=file:./.redwood/test.db

# disables Prisma CLI update notifier
PRISMA_HIDE_UPDATE_MESSAGE=true

# Get from https://filestack.com to enable image uploads in blog posts
# note: recommend setting this in .env, which is not checked into version control
# FILESTACK_API_KEY=ABCDEFG

# Set to `true` to enable /admin routes
# note: at init this is set in .env, which is not checked into version control
# USE_ADMIN=true
